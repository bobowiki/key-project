'use strict';

const keyStyledComponent = require('..');
const assert = require('assert').strict;

assert.strictEqual(keyStyledComponent(), 'Hello from keyStyledComponent');
console.info('keyStyledComponent tests passed');
